using System;

namespace Deliverable1
{
    class Program
    {
        static void Main(string[] args)
        {

            string password;
            string lower;
            string upper;
            string numeral;
            int minNum = 7;
            int maxNum = 12;
         

            Console.WriteLine("Enter a password");
            string input = Console.ReadLine();
            password = (input);


            if (password > maxNum)
            { Console.WriteLine("Error"); }

            if (password < minNum)
            { Console.WriteLine("Error"); }

            int i;
            for (i = 0; i < input; i++)
            {

                if (char.IsDigit(password))
                { Console.WriteLine("Error"); }

                if (char.IsUpper(password))
                { Console.WriteLine("Error"); }

                if (char.IsLower(password))
                { Console.WriteLine("Error"); }

                if (!char.IsLetterOrDigit(password))
                { Console.WriteLine("Error"); }

                else if
                    { Console.WriteLine("Password valid and accepted"); }
            }

        }
    }
}
